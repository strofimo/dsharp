"use strict";

define('test', ['DSharp'], function(DSharp) {
  var $global = this;

  // BasicTests.MyDebug

  function MyDebug() {
  }
  MyDebug.showInfo = function() {
  };
  MyDebug.traceInfo = function() {
  };
  MyDebug.logInfo = function() {
  };


  // BasicTests.App

  function App(i) {
    while (true) {
    }
    switch (i) {
      case 0:
        break;
    }
    i++;
    MyDebug.traceInfo();
  }
  var App$ = {

  };


  var $exports = DSharp.module('test', null,
    {
      MyDebug: DSharp.defineClass(MyDebug, null, [], null),
      App: DSharp.defineClass(App, App$, [Number], null)
    });


  return $exports;
});
