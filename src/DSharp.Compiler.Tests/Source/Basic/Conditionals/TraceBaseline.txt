"use strict";

define('test', ['ds'], function(ss) {
  var $global = this;

  // BasicTests.MyDebug

  function MyDebug() {
  }
  MyDebug.showInfo = function() {
  };
  MyDebug.traceInfo = function() {
  };
  MyDebug.logInfo = function() {
  };


  // BasicTests.App

  function App(i) {
    while (true) {
    }
    switch (i) {
      case 0:
        break;
    }
    i++;
    MyDebug.traceInfo();
  }
  var App$ = {

  };


  var $exports = ds.module('test', null,
    {
      MyDebug: ds.defineClass(MyDebug, null, [], null),
      App: ds.defineClass(App, App$, [Number], null)
    });


  return $exports;
});
