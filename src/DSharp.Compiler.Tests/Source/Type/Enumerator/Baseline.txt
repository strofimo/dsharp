"use strict";

define('test', ['DSharp'], function(DSharp) {
  var $global = this;

  // TypeTests.App1

  function App1() {
  }
  var App1$ = {
    getEnumerator: function() {
      return null;
    }
  };


  // TypeTests.App

  function App() {
  }
  var App$ = {
    test1: function(arg) {
    },
    test: function(arg) {
      var a;
      var b;
      var s = new Array(5);
      var a1 = DSharp.enumerate(a);
      a1 = DSharp.enumerate(b);
      a1 = DSharp.enumerate(s);
      var $enum1 = DSharp.enumerate(s);
      while ($enum1.moveNext()) {
        var str = $enum1.current;
        var s1 = str;
      }
      var app1;
      var $enum2 = DSharp.enumerate(app1);
      while ($enum2.moveNext()) {
        var str = $enum2.current;
        var s1 = str;
      }
    }
  };


  var $exports = DSharp.module('test', null,
    {
      App1: DSharp.defineClass(App1, App1$, [], null, [DSharp.IEnumerable]),
      App: DSharp.defineClass(App, App$, [], null)
    });


  return $exports;
});
