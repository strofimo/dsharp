"use strict";

define('test', ['ds'], function(ds) {
  var $global = this;

  // ExpressionTests.App

  function App() {
  }
  var App$ = {
    test: function(arg) {
      arg = (arg || 10);
      arg = (arg || 10 || 100);
      var s = (arg || 10).toString(10);
      var b = !!arg;
      var sb = ds.createInstance(ds.StringBuilder);
      sb = ds.createInstance(ds.StringBuilder, 'aaa');
      var i;
      var tick = function() {
        i = 10;
      };
      var cookie = setInterval(tick, 500);
      clearInterval(cookie);
      var isNotSet = !ds.isValue(i);
      b = (i === null);
      b = (i === undefined);
      b = ds.isValue(i);
      b = isNaN(0);
      b = isFinite(3);
      b = !!(0);
      b = !!b;
      b = !!(b && b);
      b = !(1);
      b = !(b && b);
      var addition = eval('2 + 2');
      addition = 2 + 2;
      addition = 2 + 3;
      var g = $global;
      var u = undefined;
      var coreModule = ds.modules['ds'];
    }
  };


  var $exports = ds.module('test', null,
    {
      App: ds.defineClass(App, App$, [], null)
    });


  return $exports;
});
