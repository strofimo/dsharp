"use strict";

define('test', ['ds'], function(ss) {
  var $global = this;

  // ExpressionTests.App

  function App() {
  }
  var App$ = {
    equality: function() {
      var d1 = null;
      var d2 = null;
      if (ds.compareDates(d1, d2)) {
      }
      if (!ds.compareDates(d1, d2)) {
      }
      if (ds.compareDates(d1, ds.now())) {
      }
      if (!ds.compareDates(d1, new Date())) {
      }
      var o;
      if (d1 === o) {
      }
      if (d1 !== o) {
      }
    }
  };


  var $exports = ds.module('test', null,
    {
      App: ds.defineClass(App, App$, [], null)
    });


  return $exports;
});
