"use strict";

define('test', ['DSharp'], function(DSharp) {
  var $global = this;

  // ExpressionTests.App

  function App() {
  }
  var App$ = {
    equality: function() {
      var d1 = null;
      var d2 = null;
      if (DSharp.compareDates(d1, d2)) {
      }
      if (!DSharp.compareDates(d1, d2)) {
      }
      if (DSharp.compareDates(d1, DSharp.now())) {
      }
      if (!DSharp.compareDates(d1, new Date())) {
      }
      var o;
      if (d1 === o) {
      }
      if (d1 !== o) {
      }
    }
  };


  var $exports = DSharp.module('test', null,
    {
      App: DSharp.defineClass(App, App$, [], null)
    });


  return $exports;
});
