"use strict";

define('test', ['ds'], function(ds) {
  var $global = this;

  // ExpressionTests.Button

  function Button() {
  }
  Button.add_test = function(value) {
    Button.__test = ds.bindAdd(Button.__test, value);
  };
  Button.remove_test = function(value) {
    Button.__test = ds.bindSub(Button.__test, value);
  };
  var Button$ = {
    add_click: function(value) {
      this.__click = ds.bindAdd(this.__click, value);
    },
    remove_click: function(value) {
      this.__click = ds.bindSub(this.__click, value);
    },
    add_aaa: function(value) {
      if (this._aaa == null) {
        this._aaa = value;
      }
      else {
        this._aaa = ds.bindAdd(this._aaa, value);
      }
    },
    remove_aaa: function(value) {
      this._aaa = ds.bindSub(this._aaa, value);
    },
    add_bbb: function(value) {
      if (this._bbb == null) {
        this._bbb = value;
      }
      else {
        this._bbb = ds.bindAdd(this._bbb, value);
      }
    },
    remove_bbb: function(value) {
      this._bbb = ds.bindSub(this._bbb, value);
    }
  };


  // ExpressionTests.App

  function App() {
    this._btn.add_click(ds.bind('_onClickButton', this));
    this._btn.add_click(ds.bind('_onClickButton', this));
    this._btn.add_click(ds.bind('_onClickButton', this));
    this._btn.remove_click(ds.bind('_onClickButton', this));
    this._btn.remove_click(ds.bind('_onClickButton', this));
    this._btn.remove_click(ds.bind('_onClickButton', this));
    Button.add_test(App._onTestButton);
    Button.add_test(App._onTestButton);
    Button.add_test(App._onTestButton);
    Button.remove_test(App._onTestButton);
    Button.remove_test(App._onTestButton);
    Button.remove_test(App._onTestButton);
    this._btn.add_aaa(ds.bind('_onAAAButton', this));
    this._btn.add_aaa(ds.bind('_onAAAButton', this));
    this._btn.add_aaa(ds.bind('_onAAAButton', this));
    this._btn.remove_aaa(ds.bind('_onAAAButton', this));
    this._btn.remove_aaa(ds.bind('_onAAAButton', this));
    this._btn.remove_aaa(ds.bind('_onAAAButton', this));
  }
  App._onTestButton = function(sender, e) {
  };
  var App$ = {
    _onAAAButton: function(sender, e) {
    },
    _onClickButton: function(sender, e) {
    }
  };


  // ExpressionTests.FooEventArgs

  function FooEventArgs() {
    ds.EventArgs.call(this);
  }
  var FooEventArgs$ = {

  };


  var $exports = ds.module('test', null,
    {
      Button: ds.defineClass(Button, Button$, [], null),
      App: ds.defineClass(App, App$, [], null),
      FooEventArgs: ds.defineClass(FooEventArgs, FooEventArgs$, [], ds.EventArgs)
    });


  return $exports;
});
